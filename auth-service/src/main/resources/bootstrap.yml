spring:
  application:
    name: auth-service
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: false # [keeping it false to make build successful]
      password: 1234
      username: user
